#include <stdio.h>
#include "minicpu.h"

/* imprime o estado da CPU a cada ciclo. */
void trace(CPU *cpu, uint8_t op, uint8_t a, uint8_t b) {
    (void)cpu; (void)op; (void)a; (void)b;
    /* TODO: Pedor
     */
}
